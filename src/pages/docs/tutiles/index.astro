---
import MainLayout from "~/layouts/MainLayout.astro"

const docs = await Astro.glob("./*.md")
---

<MainLayout title="Tarjetas Útiles">
  {docs.map((doc) => (
    <article>
      <a href={doc.url}>{doc.frontmatter.title}</a>
    </article>
  ))}
</MainLayout>
